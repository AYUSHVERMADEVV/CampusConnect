{
  "name": "campusconnect",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run build --workspace=client && node server/server.js",
    "build": "npm run build --workspace=client",
    "start": "node server/server.js",
    "lint": "npm run lint --workspace=client"
  }
}
